<!DOCTYPE html>
<html>
  <head>
    <title><%= [@title, 'Puffer'].compact.join(' - ') %></title>
    <meta http-equiv="content-type" content="text/html;charset=utf-8" />
    <%= csrf_meta_tag %>
    <%= stylesheet_link_tag    "puffer" %>
    <%= javascript_include_tag "puffer" %>
    <%= yield :stylesheets %>
    <%= yield :javascripts %>
  </head>
  <body>
    <div class="body">
      <div class="header">
        <div class="logo">
          <span><%= t('puffer.logo') %></span>
        </div>
      </div>
      <div class="content sessions">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
